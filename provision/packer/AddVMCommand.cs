using System;
using System.IO;
using Delineate.Fast;

namespace Delineate.Fast.Provision.Packer
{
    [CommandMatch(new String[]{"vm", "add"} )]
    public class AddPackerVmCommand : ProvisionCommand
    {
        protected override void Plan(CommandArgs args)
        {
            
        }

        protected override void Apply(CommandArgs args)
        {
            
        }    
    }
}